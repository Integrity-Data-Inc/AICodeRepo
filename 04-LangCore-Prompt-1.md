Create an ASP.NET Core Web API in `LangCore` project with:

- POST /validate : accepts JSON, returns ValidationResult[]
- POST /query : accepts JSON + question, returns string

Use clean controller + service + prompt design